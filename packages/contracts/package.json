{
  "name": "@agently/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Canonical domain model, validation schemas, events, and API contract. Framework-agnostic; shared by web, services, worker, and a future standalone API.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
